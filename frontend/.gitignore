node_modules

# Production build output — regenerated by `npm run build`.
/build

# Secrets / environment — never commit real values (see .env.example).
.env
.env.local
.env.*.local

# Stray packaged artifacts — not source, shouldn't be tracked.
*.zip

npm-debug.log*
coverage/

